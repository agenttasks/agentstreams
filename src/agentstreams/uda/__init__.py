"""UDA (Unified Data Architecture) core — schema registry, Avro codegen, GraphQL types.

Follows Netflix UDA pattern: a single ontology produces multiple representations
(Avro for serialization, GraphQL for API, TTL for semantic web, SQL for storage).
"""
