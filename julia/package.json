{
  "name": "julia",
  "version": "0.1.0",
  "private": true,
  "description": "Legal AI agent — Harvey replacement with zero Harvey dependencies. Vault, completion, client matters, audit, assistant, MCP server.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "check": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint src/ --ext .ts"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.2.97",
    "@anthropic-ai/sdk": "^0.86.1",
    "@modelcontextprotocol/sdk": "^1.12.0",
    "@neondatabase/serverless": "^1.0.2",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^22",
    "typescript": "^5.8",
    "vitest": "^3.2.0"
  }
}
